from PACKET import *PACKETNAME = "PARTICLEGAME"class PARTICLEGAME(PACKET):    def __init__(self):        PACKET.__init__(self)        self.packname = "PARTICLEGAME"        self.packinfo = "A new high performance Particle simulator beta (very early preview)"        self.packversion = 1.1    def setup(self, args=None):        PACKET.setup(self)        self.download_dependency("dist.zip")        self.unzip(DATAFOLDER2 + "EXTERNPACKAGES\\" + self.packname,"\\dist.zip")    def run(self):        os.system("cd " + DATAFOLDER + "EXTERNPACKAGES/" + self.packname + "/ & start main.exe")        